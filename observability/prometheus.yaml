global:
  scrape_interval: 30s
  evaluation_interval: 30s
rule_files:
  - /etc/prometheus/host-rules.yaml
scrape_configs:
  - job_name: otel-collector
    honor_timestamps: true
    static_configs:
      - targets: ['otel-collector:9464']
